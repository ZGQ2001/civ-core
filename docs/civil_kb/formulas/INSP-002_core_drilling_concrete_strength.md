---
formula_id: INSP-002
title: 钻芯法检测混凝土抗压强度推定值计算逻辑
code_ref: ["JGJ/T 384-2016"]
business_type: 现场检测与鉴定
inputs:
  - f_cu_cor_values: List[float]  # 单个芯样试件抗压强度值列表（MPa），精确至0.1MPa
  - n: int                         # 芯样试件数量
outputs:
  - f_cu_cor_m: float              # 芯样试件抗压强度平均值（MPa），精确至0.1MPa
  - S_cu: float                    # 芯样试件抗压强度样本标准差（MPa），精确至0.01MPa
  - f_cu_e1: float                 # 混凝土抗压强度推定上限值（MPa），精确至0.1MPa
  - f_cu_e2: float                 # 混凝土抗压强度推定下限值（MPa），精确至0.1MPa
  - f_cu_est: float                # 检测批混凝土抗压强度推定值（MPa），取推定区间上限值或下限值
  - k1: float                      # 推定区间上限值系数
  - k2: float                      # 推定区间下限值系数
---

> 依据《钻芯法检测混凝土强度技术规程》JGJ/T 384-2016 第 6.3.2 条及附录 A。
> 检测批的混凝土抗压强度推定值应计算推定区间，推定区间上限值和下限值按下列公式计算。

---

## 1. 芯样试件抗压强度平均值

$$f_{cu,cor,m} = \frac{1}{n} \sum_{i=1}^{n} f_{cu,cor,i}$$

- $f_{cu,cor,i}$ — 单个芯样试件抗压强度值（MPa），精确至 0.1 MPa
- $n$ — 芯样试件数量
- $f_{cu,cor,m}$ — 芯样试件抗压强度平均值（MPa），精确至 0.1 MPa

---

## 2. 芯样试件抗压强度样本标准差

$$S_{cu} = \sqrt{\frac{\sum_{i=1}^{n} (f_{cu,cor,i} - f_{cu,cor,m})^2}{n - 1}}$$

结果精确至 0.01 MPa。

---

## 3. 推定区间计算

### 推定上限值

$$f_{cu,e1} = f_{cu,cor,m} - k_1 \cdot S_{cu}$$

### 推定下限值

$$f_{cu,e2} = f_{cu,cor,m} - k_2 \cdot S_{cu}$$

结果精确至 0.1 MPa。

---

## 4. 推定值系数

依据 **JGJ/T 384-2016 附录 A**，系数 $k_1$、$k_2$ 根据检测批样本容量 $n$ 和置信水平确定。以下为实际工程中出现的两组取值示例：

| 场景 | $k_1$ | $k_2$ | 适用条件 |
|------|-------|-------|---------|
| 示例 A | 1.11397 | 2.56600 | 较小样本容量 |
| 示例 B | 1.17485 | 2.20778 | 较大样本容量 |

> **注意**：上述仅为特定工程的取值示例。实际使用时必须根据芯样数量 $n$ 查阅 **JGJ/T 384-2016 附录 A** 完整系数表，不可直接套用。
>
> 规律：$k_1$ 随 $n$ 增大而增大，$k_2$ 随 $n$ 增大而减小；$n$ 越小推定区间越宽。

---

## 5. 检测批推定值确定

根据 JGJ/T 384-2016 第 6.3.2 条，检测批混凝土抗压强度推定值可取推定区间的上限值或下限值：

$$f_{cu,est} = \begin{cases} f_{cu,e1} & \text{取上限值} \\ f_{cu,e2} & \text{取下限值} \end{cases}$$

---

## 6. Excel 计算参考

| 步骤 | Excel 等价 |
|------|-----------|
| 平均值 | `=AVERAGE(范围)` |
| 标准差 | `=STDEV.S(范围)` |
| 推定上限 | `=AVERAGE(范围) - k1 * STDEV.S(范围)` |
| 推定下限 | `=AVERAGE(范围) - k2 * STDEV.S(范围)` |
